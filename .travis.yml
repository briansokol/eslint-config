language: node_js
node_js:
  - stable

install:
  - yarn install

script:
  - yarn run lint:react
  - yarn run lint:node
  - yarn run lint:react-native

deploy:
  provider: npm
  email: '$NPM_EMAIL'
  api_key: '$NPM_TOKEN'
  on:
    tags: true
    repo: briansokol/eslint-config
    branch: master
